name: Unit-test Lookout microservice

on:
  pull_request:
    branches:
      - 'uat'
      - 'main'
    paths:
      - "internal/lookout/**"

jobs:
  build:
    env:
      PRODUCTION: false
    runs-on: ubuntu-latest
    steps:
      #Checkout repo
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Golang
        uses: actions/setup-go@v2
        with: 
          go-version: 1.18

      - name: Build Lookout uService
        working-directory: ./internal/lookout
        run: go build .

      - name: Run Tests
        working-directory: ./internal/lookout
        env:
          FIREBASE_KEY: ${{ secrets.FIREBASE_DEV_PRIVATE_KEY }}
          FIREBASE_KEY_ID: ${{ secrets.FIREBASE_DEV_PRIVATE_KEY_ID }}
          MONGO_URI: ${{ secrets.MONGO_URI_DEV }}
          BONSAI_ES_URI: ${{ secrets.BONSAI_ES_URI }}
        run: go test .